--- upstream/code-review/SKILL.md
+++ ours/code-review/SKILL.md
@@ -3 +3 @@
-description: "Review the changes since a fixed point (commit, branch, tag, or merge-base) along two axes: Standards (does the code follow this repo's documented coding standards?) and Spec (does the code match what the originating issue/spec asked for?). Runs both reviews in parallel sub-agents and reports them side by side. Use when the user wants to review a branch, a PR, work-in-progress changes, or asks to \"review since X\"."
+description: "Review the changes since a fixed point (commit, branch, tag, or merge-base) along two axes: Standards (does the code follow this repo's documented coding standards?) and Spec (does the code match what the originating issue/spec asked for?). Runs both reviews independently and reports them side by side. Use when the user wants to review a branch, a PR, work-in-progress changes, asks to \"review since X\", or invokes /review or /code-review."
@@ -11 +11 @@
-Both axes run as **parallel sub-agents** so they don't pollute each other's context, then this skill aggregates their findings.
+Both axes run **independently** so they don't pollute each other's context: as parallel reviewer agents in Herdr when `herdr_subagent` is available, or as two self-contained sequential passes otherwise. This skill then aggregates their findings.
@@ -23 +23 @@
-Before going further, confirm the fixed point resolves (`git rev-parse <fixed-point>`) and the diff is non-empty. A bad ref or empty diff should fail here, not inside two parallel sub-agents.
+Before going further, confirm the fixed point resolves (`git rev-parse <fixed-point>`) and the diff is non-empty. A bad ref or empty diff should fail here, not inside the two independent reviews.
@@ -32 +32 @@
-4. If nothing is found, ask the user where the spec is. If they say there isn't one, the **Spec** sub-agent will skip and report "no spec available".
+4. If nothing is found, ask the user where the spec is. If they say there isn't one, the **Spec** review will skip and report "no spec available".
@@ -58 +58 @@
-### 4. Spawn both sub-agents in parallel
+### 4. Run both reviews independently
@@ -60 +60,3 @@
-**Standards sub-agent prompt** should include:
+If `herdr_subagent` is available, call its `status` action, then launch two `reviewer` agents in parallel with `thinking: high`. Keep the reviewer role's default model so the repository's routing policy remains authoritative. Continue from the automatic completion notifications. Inspect both panels before trusting their results, then close them after harvesting the reports. Otherwise, run the two briefs yourself as self-contained sequential passes. Finish one axis before starting the other, and build each report only from that axis's brief so one review cannot steer the other.
+
+**Standards review prompt** should include:
@@ -63 +65 @@
-- The list of standards-source files you found in step 3, **plus the smell baseline from step 3** pasted in full (the sub-agent has no other access to it).
+- The list of standards-source files you found in step 3, **plus the smell baseline from step 3** pasted in full. The reviewer must receive the complete axis-specific brief.
@@ -66 +68 @@
-**Spec sub-agent prompt** should include:
+**Spec review prompt** should include:
@@ -72 +74 @@
-If the spec is missing, skip the Spec sub-agent and note this in the final report.
+If the spec is missing, skip the Spec review and note this in the final report.
