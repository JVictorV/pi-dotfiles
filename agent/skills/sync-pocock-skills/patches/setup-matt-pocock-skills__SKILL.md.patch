--- upstream/setup-matt-pocock-skills/SKILL.md
+++ ours/setup-matt-pocock-skills/SKILL.md
@@ -24 +24 @@
-- `AGENTS.md` and `CLAUDE.md` at the repo root: does either exist? Is there already an `## Agent skills` section in either?
+- `AGENTS.md` and `CLAUDE.md` at the repo root: does either exist? Is there already an `## Agent skills` section in either? Prefer `AGENTS.md` when both exist.
@@ -67 +67 @@
-- The `## Agent skills` block to add to whichever of `CLAUDE.md` / `AGENTS.md` is being edited (see step 4 for selection rules)
+- The `## Agent skills` block to add to whichever of `AGENTS.md` / `CLAUDE.md` is being edited (see step 4 for selection rules)
@@ -76,2 +76,2 @@
-- If `CLAUDE.md` exists, edit it.
-- Else if `AGENTS.md` exists, edit it.
+- If `AGENTS.md` exists, edit it.
+- Else if `CLAUDE.md` exists, edit it.
