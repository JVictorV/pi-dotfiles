--- upstream/codebase-design/DESIGN-IT-TWICE.md
+++ ours/codebase-design/DESIGN-IT-TWICE.md
@@ -3 +3 @@
-When the user wants to explore alternative interfaces for a chosen deepening candidate, use this parallel sub-agent pattern. Based on "Design It Twice" (Ousterhout): your first idea is unlikely to be the best.
+When the user wants to explore alternative interfaces for a chosen deepening candidate, use this parallel `herdr_subagent` critic-agent process. Based on "Design It Twice" (Ousterhout): your first idea is unlikely to be the best.
@@ -11 +11 @@
-Before spawning sub-agents, write a user-facing explanation of the problem space for the chosen candidate:
+Before launching the design agents, write a user-facing explanation of the problem space for the chosen candidate:
@@ -17 +17 @@
-Show this to the user, then immediately proceed to Step 2. The user reads and thinks while the sub-agents work in parallel.
+Show this to the user, then immediately proceed to Step 2. The user reads and thinks while the design agents work in parallel.
@@ -19 +19 @@
-### 2. Spawn sub-agents
+### 2. Launch parallel design agents
@@ -21 +21 @@
-Spawn 3+ sub-agents in parallel. Each must produce a **radically different** interface for the deepened module.
+If `herdr_subagent` is available, call its `status` action, then launch 3+ `critic` agents in parallel with `thinking: high`. Keep each role's default model. Give every agent the original technical brief and one distinct design constraint. If the tool is unavailable, produce the alternatives as isolated sequential passes from the original brief so one design does not anchor the next.
@@ -23 +23 @@
-Prompt each sub-agent with a separate technical brief (file paths, coupling details, dependency category from [DEEPENING.md](DEEPENING.md), what sits behind the seam). The brief is independent of the user-facing problem-space explanation in Step 1. Give each agent a different design constraint:
+The technical brief must include file paths, coupling details, the dependency category from [DEEPENING.md](DEEPENING.md), and what sits behind the seam. It is independent of the user-facing problem-space explanation in Step 1. Use these design constraints:
@@ -25,4 +25,4 @@
-- Agent 1: "Minimize the interface: aim for 1–3 entry points max. Maximise leverage per entry point."
-- Agent 2: "Maximise flexibility: support many use cases and extension."
-- Agent 3: "Optimise for the most common caller: make the default case trivial."
-- Agent 4 (if applicable): "Design around ports & adapters for cross-seam dependencies."
+- Critic 1: "Minimize the interface: aim for 1–3 entry points max. Maximise leverage per entry point."
+- Critic 2: "Maximise flexibility: support many use cases and extension."
+- Critic 3: "Optimise for the most common caller: make the default case trivial."
+- Critic 4 (if applicable): "Design around ports & adapters for cross-seam dependencies."
@@ -30 +30 @@
-Include both [SKILL.md](SKILL.md) vocabulary and CONTEXT.md vocabulary in the brief so each sub-agent names things consistently with the architecture language and the project's domain language.
+Include both [SKILL.md](SKILL.md) vocabulary and CONTEXT.md vocabulary in the brief so each critic agent names things consistently with the architecture language and the project's domain language.
@@ -32 +32 @@
-Each sub-agent outputs:
+Each critic agent outputs:
@@ -38,0 +39,2 @@
+
+Rely on automatic completion notifications. Inspect every agent panel before using its result, then close the panels after harvesting the designs.
